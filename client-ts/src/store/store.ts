import Vue from 'vue';
import Vuex from 'vuex';
Vue.use(Vuex);

// import advisorTableState, { AdvisorTableState } from './modules/advisor-table';

const store = new Vuex.Store({});

export default store;
