import { User, UserModel, Game, GameModel } from '../db';
import { verifyJwt, createLogger, sleep, randomString } from '../../shared';
const logger = createLogger('game.state.ts');

class GameState{

}